[build]
builder = "DOCKERFILE"

[deploy]
# Caddy startup script is in Dockerfile - don't override it
# startCommand is removed so Dockerfile CMD runs (starts Caddy + FastAPI)
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10 